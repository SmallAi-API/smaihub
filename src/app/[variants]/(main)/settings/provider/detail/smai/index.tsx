'use client';

import { SMAIProviderCard } from 'model-bank/modelProviders';

import ProviderDetail from '../default';

const Page = () => {
  return <ProviderDetail {...SMAIProviderCard} />;
};

export default Page;
