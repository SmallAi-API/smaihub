'use client';

import { NewAPIProviderCard } from 'model-bank/modelProviders';

import ProviderDetail from '../default';

const Page = () => {
  return <ProviderDetail {...NewAPIProviderCard} />;
};

export default Page;
